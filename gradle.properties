# --- Publishing metadata (keep as-is)
POM_ARTIFACT_ID=library
POM_NAME=Chuck
POM_PACKAGING=aar

# --- REQUIRED for AndroidX (this fixes your build)
android.useAndroidX=true
android.enableJetifier=true

# --- Optional but recommended (removes JitPack warning)
android.suppressUnsupportedCompileSdk=36
